decl printInt(i:Int):Void
decl printString(s:String):Void

def main():Int { 
	n:Int = 30;
	printInt(fib(n));
	return 0;
}


def fib(n:Int):Int {
	if (n <= 1) {
		return n; 
	} else {
		return fib(n-1) + fib(n-2); 
   }
} 

